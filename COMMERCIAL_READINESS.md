# Commercial Readiness

本项目当前处于功能内测阶段，不是已完成的商业发行版。以可验证的真实流程推进；保留原 UI，不将占位界面、模拟数据或协议测试当作已上线功能。

## 已闭环

### 2026-09-18 Agent 档案更新

后续产品方向已按用户要求收简：前台不提供 Agent 目录、创建或选择，不建设 Agent 软件安装中心。仅在 Hermes 设置内提供 default 档案的协作权限、执行与分工限制；聊天继续以 Hermes 为唯一入口。以下档案管理描述为底层适配能力，不代表前台开放的功能。

新增 Agent 档案读取/创建、职责与角色设定、档案级模型和工具集配置、执行与委派参数，以及本账户工作台接单开关。会话持久绑定 Agent；创建/恢复/发送/中断/核对按档案路由，不切全局 active。详见 `server/AGENT_PROFILES.md`。下文“暂仅支持 default 档案”仅适用于尚未迁移的记忆、语音、Skills/MCP 和定时任务管理页。真实 NAS 只读核查，写入与非默认档案推理由隔离测试验证，不能标记为真实服务验收完成。

项目会话绑定和结构化任务提案也已落地，取代下文早期“未实现会话项目归属”的记录；当前仍是授权快照与提案协议，不是实时业务工具桥。历史重命名/分类、日程/周/月视图已实现。健康与 Google 数据源仍未接入。

| 链路 | 实现与验证 |
| --- | --- |
| 平台账户 | 注册、登录、会话撤销、用户隔离、CSRF；线上验证服务签名已验证，用户实际注册与登录仍需真实账号验收 |
| 任务与日历 | 同一记录、持久化、冲突检测、幂等写入、跨窗口与增量读取 |
| Hermes 连接 | 每用户登录、加密 cookie、模型目录发现；指定 NAS 实测通过 |
| 对话执行 | WS 鉴权、创建/恢复会话、提交、流式输出、中断、持久化执行记录、SSE 重连 |
| 审批 | 网关明确发来的 approval/clarify 请求；展示命令和参数，用户答复，过期校验；不支持的结构转交远端处理 |
| 异常恢复 | 断线/重启/超时保留 unknown，禁止自动重发；核对远端 idle 后结束跟踪，不冒充拿到了完整结果 |
| 备份 | SQLite 在线备份、配套 vault.key、校验清单、完整性校验与恢复读取测试 |
| 文档工作空间 | 服务端存储、文本导入/编辑、Markdown 预览、20 个版本、回收站/恢复/彻底删除、双窗口冲突保护、重试幂等；文档附回对话，完成回复保存为文档 |
| 设置与国际化 | 五种主题、设备级持久化、减少动画、中文/英文、登录语言选择；源码静态字面量编译与独立词典，不翻译用户内容；主页面与移动端回归 |
| 远端管理 | 工具集/Skills/MCP/记忆配置/定时任务/语音配置只读实测；确认后 Skills/MCP 启停及定时任务暂停/恢复，修改接口经协议测试，不擅自修改真实 NAS |
| Windows | Electron x64 便携构建；服务器选择、多窗口、持久登录、隔离 renderer、无远端 preload；不包含服务端密钥；尚未签名或提供自动更新 |
| UI | 原版布局；真实助手和执行中心；小幅状态动画、减少动态效果支持；桌面与移动测试 |

NAS 实测覆盖登录、模型目录、WS 鉴权与 ready 握手，以及管理接口只读检查（29 个工具集、201 个 Skills、5 个 MCP 服务、3 个定时任务，记忆和语音配置）。完整对话/审批/中断及管理修改由协议测试验证；尚未在用户 NAS 上请求模型推理或擅自启停资源。一次真实任务验收仍是发布门槛。

## 执行边界

- 模型管理现已支持刷新实际目录、搜索、确认应用到共享 default 档案，以及远端回读验证。只影响新会话，不承诺旧会话热切换；聊天中的旧会话不再显示全局默认模型冒充其实际模型。应用前检查旧模型，单实例内串行配置写入；远端无 CAS，仍有外部客户端竞争窗口。高费用模型授权单独勾选，默认不授权。真实 NAS 仅核查 OpenAPI，未代用户切换模型。
- Skills Hub 现支持官方来源搜索（最多 50 条，每页 10 条）、正文和文件清单预览、安全扫描、确认安装及来源回读验证。只有 safe + allow 且无 high/critical 发现才可安装；审查凭据绑定用户和连接、10 分钟过期、安装前重新扫描，提交后凭据消费，未知结果禁止盲重试。远端安装可能立即启用技能，须确认共享档案和执行风险。上游 API 不支持固定包摘要，复扫与实际安装仍存在来源变化窗口，不能宣传为供应链隔离。外部 URL/任意仓库安装、升级卸载、持久化安装作业仍未完成。真实 NAS 已验证搜索、预览、扫描；实际安装仅隔离测试，无真实服务器修改。

- 新增人工审核的回复任务清单导入：仅接受本账户已成功完成回复中的 Markdown 未完成复选项，最多 30 项；提交须显式确认。任务创建、项目关联、来源记录与导入回执在同一事务完成，重试幂等，同一来源项不重复创建；修改任务保留来源。测试覆盖隔离、CSRF、无确认拒绝、回滚、重复、重启和桌面/移动/英文夜间模式。不会猜测自然语言日期，不自动执行回复中的指令，也不是远端 MCP 工具桥。真实 NAS 模型生成清单的端到端验收尚未执行。

- 项目工作空间已接入共享任务/文档关联、原子新建任务关联、版本冲突恢复及归档；项目为账户内组织容器，不是多人租户权限系统。没有新增独立日历事件或时间块模型。项目上下文仅为显式确认的只读快照，未实现 Hermes 实时读写业务工具、会话项目归属或自动产物归档。项目元数据通过刷新加载，不承诺实时跨设备推送。新增浏览器回归覆盖创建、关联、完成、文档跳转、上下文附件、并发修改恢复、归档和移动布局。

- 首次发送明确确认本会话执行权限，授权持久化，可在同页上下文面板撤销。新会话重新授权；工具审批仍独立确认。默认不附带工作台任务、健康或邮件。
- 已支持用户选择或拖入 Markdown、UTF-8 文本和代码附件，随消息保存并作为参考文本发送至网关；每个 64 KB，最多 5 个，总计 192 KB。不支持 PDF、图片、Office，也未实现远端文件系统上传。附件按账户隔离，目前数据库内明文保存，需纳入数据保留与删除策略。
- 远端 Agent 可按其已有工具策略执行；工作台不是远端沙箱，不能强制所有工具都审批。不能将本地审批 UI 宣传为完整执行隔离。
- 工作台任务与日历可在工具权限页显式选择并附加只读快照，附带任务版本与采集时间；不是实时工具调用，也不授予模型自动写入权限。文档仍通过显式附件共享。健康和外部邮箱不共享。
- 远端配置仅返回字段白名单，避免暴露密钥或进程环境。记忆开关/字符上限/压缩、语音服务商/识别开关/音色支持确认后保存及回读验证；修改失败保留草稿，结果不明时不自动重试。default 档案为共享配置，不是隔离沙箱。NAS OpenAPI 确认配置 PUT 深度合并：仅发送修改字段，绝不回写读取的凭证或其他配置。保存前校验版本，同一工作台实例按服务器串行写入，但外部客户端仍有检查后写入竞争，不能宣称原子并发保护。写入尚未在真实 NAS 验收，也不保证远端热重载。
- 能力中心展示真实已安装列表及 MCP 目录；确认后支持目录内无需密钥、无需 bootstrap 的 HTTP 条目安装，默认 enable:false，回读 installed/enabled 后才确认成功；随后可连接 OAuth 账号、检查授权、验证并启用或移除。额外环境变量、命令型 MCP 尚未接入。Skills Hub 官方来源安装已接入上述审查流程，外部来源和持久化安装状态跟踪仍待完成。工具集编辑仍未开放。不将配置安装等同于工具可调用。
- 回复朗读接入 `/api/audio/speak`，使用远端已配置音色，仅接受受限音频 data URL；协议测试通过，未在 NAS 实测合成。连续 ASR/TTS 对话、打断、音色列表未完成。Windows 端目前默认拒绝设备权限，未宣称麦克风可用。
- 暂仅支持 `default` 档案，使用远端会话模型。可应用共享默认模型到新会话，不能宣传为现有会话热切模。
- 每用户最多一个进行中或未确认任务，防止冲突。幂等键持久化；相同键不同内容被拒绝。
- 重复点击、断线、丢失 ACK 不触发自动重提；未知结果可核对远端状态。必须显式确认才能结束本地跟踪，该操作不会停止远端执行。
- 多问题澄清、无选项审批、过长的无法完整显示的审批，暂不接受本地授权；用户在服务器处理或中断。
- 对话历史同页分页加载、独立滚动，输入框固定；Markdown 采用 marked 与 DOMPurify 白名单渲染，禁用脚本、图片和嵌入内容。文件支持预览、源码和下载；HTML 附件仅显示源码。不保存或展示推理链事件，尚未实现远端工具产物管理。
- 网页退出不会自动中断已授权的后台任务。会话撤销会结束对应 SSE 订阅；再次登录可查看本账户记录。
- 数据库为单进程单实例；多实例需要共享队列、分布式互斥、Postgres 与消息分发，不能直接复制多个 Node 实例共同运行。
- 文档为 UTF-8 文本，每份 64 KB，每账户 200 份（含回收站），历史快照合计 20 MiB。保留最近 20 个版本；彻底删除清除当前逻辑内容和版本，保留无正文 tombstone 防止延迟请求复活。聊天附件、远端副本及既有备份独立保留，不宣称安全擦除磁盘或跨系统删除。编辑草稿只在当前页面内存中，离页提示未保存，尚无离线草稿同步与多人合并。

## 发布门槛

本轮新增能力分类/搜索/分页、外部 HTTPS MCP 添加及连接页管理、本地全量会话检索。外部 MCP 目前仅 none/OAuth，不支持密钥/命令型导入；第三方数据同步、外部 Skill 包安装仍是未完成项。Asana 已实测因 NAS MCP 依赖缺少 HTTP 传输失败，工作台修正了错误分类，但不能代替服务器依赖修复。参考仓库采用 BSL-1.1，未复制其商业受限实现。

MCP 目录已补充 OAuth 发起/检查/取消、连接验证后启用、移除回读。授权链接仅允许 HTTPS，不转发 token；本地流程按账户与连接隔离，页面刷新可恢复，服务端重启不可恢复。仍需真实第三方 OAuth 验收、自动状态推送、持久化安装任务和自定义密钥/命令型 MCP 配置，不应视为完整商业模块系统。

1. 真实 NAS 上完成多轮消息、失败、中断、审批超时、断线恢复和长时间任务验收；固定并公布支持的网关版本与能力矩阵。
2. 应用用户找回密码：验证服务新接口、已验证联系方式、一次性令牌、发送限流、找回后所有会话撤销。不得借用开发者账号找回接口。
3. 完整账户生命周期：邮箱/手机号验证、注销、数据导出、删除及备份保留策略、审计、滥用处理、订阅权益和支付回调验签。
4. 数据接入：Google Tasks/Calendar/Gmail OAuth，增量同步、撤权、删除映射与冲突处理；原生健康授权最小化。
5. 工具与模块：manifest/schema、安装升级卸载、签名、版本固定、按用户 scopes、执行沙箱与审计，不直接运行任意未隔离 Skill。
6. 代码执行与远端产物：文本文档工作空间已接入，但尚无仓库管理、用户隔离执行根、版本 diff、PTY 限权、远端产物下载与病毒扫描；不提供真实代码运行环境。
7. 模型与语音：能力协商、会话级模型设置，ASR/TTS 提供商、音色、打断、回声处理；不以浏览器单次听写替代连续 AI 语音。
8. 可靠性：负载/长稳/故障注入测试、SLO、配额、成本预算、日志脱敏、指标与告警、自动备份和异地恢复演练、版本回滚。
9. Windows/Android/iOS：Windows 已有未签名便携客户端，但仍需签名安装包、更新回滚、崩溃收集和安全审计。Android/iOS 原生客户端、后台与系统通知、隐私清单和商店审核未完成。

## 验收原则

每一项需包含：成功路径、失败反馈、取消与重试、权限与账户隔离、持久化与恢复、跨端状态、自动化测试和真实服务验证。未通过的能力保持未接入或内测标记，不宣传为完整商用。
