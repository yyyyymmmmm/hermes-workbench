# 个人工作台完整方案

## 0. 整体闭环与改造基线（2026-09-18 修订）

本节是当前实施基线；后文保留早期方案供追溯。所有“目标、建议、新增”均非已完成功能。旧文中的 Flutter 默认路线、侧栏助手定位和 P0 范围不能直接当作当前实施决定。

### 0.1 产品定义与边界

工作台是管理个人事项、资料、时间和执行过程的系统；Hermes 是可接入、可替换的执行引擎，不是整个产品的数据中心。即使模型、NAS 或外部服务离线，用户仍应能管理自己的本地工作内容。该离线能力是目标，现阶段网页尚未具备完整离线写入队列。

核心闭环：收集 → 理解 → 计划 → 确认 → 执行 → 保存成果 → 更新进度 → 安排后续 → 复盘。

互通不等于模块能任意访问彼此数据库，也不等于 AI 自动获得所有数据。所有入口必须调用同一套业务服务；上下文和操作权限按用户、工作空间、数据类型、具体资源划分。

### 0.2 当前实现与根本差距

| 领域 | 当前实现 | 必须改造的部分 |
| --- | --- | --- |
| 对话 | 网关流式执行、历史、附件、审批、搜索 | 会话与项目归属、独立草稿、置顶归档、模型实际生效、结构化工具成果 |
| 工作空间 | 文本文档、版本、回收站 | 真正的项目容器；关联会话、任务、事件、文件和执行环境 |
| 待办与日历 | 同一任务记录含安排时间 | 独立建模任务、截止时间、会议和时间块；保留统一视图与关联 |
| 数据共享 | 显式附件与任务快照 | 受控工作台工具；实时读取版本、变更提案、用户确认和执行回执 |
| 能力 | 目录、部分 MCP 添加/授权/验证、列表管理 | 能力注册、兼容性检查、安装任务、版本和权限生命周期 |
| 连接与数据 | Hermes 连接、MCP 管理 | Google 等原生连接器、后台同步、凭证生命周期、冲突中心 |
| 账户与多端 | 登录、设备会话、基础同步、Windows 客户端 | 找回/注销/导出、完整离线、原生手机端、后台通知、发行安全 |

现有备份、版本冲突、幂等、账户隔离和测试需要保留，不应为换框架而推倒重建。NAS 已确认的 MCP HTTP 依赖故障属于部署兼容问题，UI 改造不能代替修复。

### 0.3 一份数据，多种视图

统一实体及职责：

- Account / Device：身份、会话和设备，不与第三方服务凭据混为一谈。
- Workspace / Project：长期事项的组织与授权边界；个人版可先使用默认空间，不强制每条临时事项建项目。
- Task：需要完成的事项，持有状态、截止时间、优先级、负责人和项目关系。
- CalendarEvent：会议、预约等事件，持有参与者、日期/时区、重复规则和外部来源。
- TimeBlock：某次计划工作时间；可关联 Task，一个任务允许多个时间块。
- Document / Asset：文档及附件，持有版本、内容类型、来源和存储引用。
- Conversation / Message：会话及消息，关联空间、项目、明确选入的上下文与运行时。
- ActionProposal / AgentRun / Artifact：拟议操作、真实执行和产物；聊天中的一句“完成了”不是执行证据。
- Connection / ExternalMapping / SyncCursor：外部账户、对象映射、远端版本和同步游标。
- Capability / Installation / PermissionGrant：能力定义、安装实例、实际授予的访问范围。
- Memory / Preference：可编辑的长期记忆与明确设置；不能把所有会话内容自动写入永久记忆。
- HealthSummary：按授权聚合的健康摘要，默认不进入通用 AI 上下文。

实体共享稳定 ID、owner、workspace、版本、来源、更新时间及删除语义；并非每个字段适用于每种实体。使用显式关系关联对象，不以名称或复制正文代替关联。优先在 SQL 中建立清晰的领域表，不为“万物关联”引入图数据库。

任务列表、日历、今天页面和项目进度是同一业务数据的不同视图。完成任务应更新任务状态和相关视图；不自动删除历史时间块、不把未发生的计划时间当成实际工作时长。移动时间块不改变任务截止日期，也不自动移动外部会议。

外部系统对外部资源保持权威；本地存储是明确映射的副本或缓存。断开连接时由用户选择保留导入副本或删除，不默认删除外部数据。

### 0.4 统一业务调用链

```text
页面操作 / Hermes 工具 / 定时工作流 / 外部同步
                    |
       统一身份、权限、参数与版本校验
                    |
      任务 / 日历 / 资料 / 项目等业务服务
                    |
         数据库事务 + 持久化 outbox
                    |
   同步适配器 / 搜索更新 / 通知 / 客户端刷新
```

工作台向 Hermes 暴露版本化工具契约，例如 search_items、get_task、propose_task_changes、apply_approved_changes；这些是拟议接口，不是已存在的 Hermes 官方端点。工具名和协议最终以实际适配验证为准。

Hermes 不能直接写 SQL，前端不能直连第三方管理凭据，Skill 不能绕过权限服务。直接编辑和 AI 编辑使用同一领域命令。一次业务变更形成可追溯事件，由订阅者更新其他视图，禁止模块相互调用形成循环写入。

每次写入携带 operationId、actor、资源 ID、预期版本、授权范围、来源和关联执行 ID。数据库事务同时保存变更与 outbox；后台消费者按事件 ID 去重。外部同步保留来源标记和远端修订，避免导入→导出→再次导入循环。

模型返回文本只能视为建议。批准的结构化提案包含明确操作集合和版本；提交时重新检查权限及数据是否变化，过期的批准不能用于新操作。能用同一数据库事务完成的变更原子提交；跨外部系统的多步操作必须展示部分成功，不宣称整体原子成功。

### 0.5 上下文、权限与执行状态

上下文来自当前项目、用户选入的资源和经过授权的检索结果；发送前显示来源与范围。大型文件按需要检索片段，保留来源、版本与引用，不能把整个工作台全部塞进提示词。

权限区分读取、修改、删除、对外发送、代码执行、健康数据访问及费用风险。授权可以限定本次、会话或空间；长期授权可撤销并有期限。邮件发送、外部共享、删除、付款或有风险的命令默认逐项确认。不能用工作台的总开关代替远端工具自己的安全限制。

状态必须反映真实结果：草拟、待确认、排队、执行中、部分成功、成功、失败、取消中、已取消、结果未知。网络超时不等于失败；远端可能已完成时先核对，不自动重试非幂等操作。停止生成、取消工作流与终止远端命令是三种不同动作。

应用删除或恢复可提供版本撤销；发送邮件、外部删除等不可逆动作不能虚构撤销按钮。移除 MCP 配置不等于撤销第三方 OAuth；两者需要分别管理。

所有授权检查由服务端强制执行。若 Hermes 直接使用广泛授权的远端工具，工作台无法保证其操作都经过本地审批；商业托管模式必须提供隔离运行环境、受控工具出口，或明确禁止不受管的调用。自托管管理员模式单独标识。

### 0.6 四条跨功能验收流程

1. 项目交付：导入需求文档 → 新建项目 → Hermes 提出任务分解与时间块 → 用户确认 → 任务和日历同时更新 → 产物保存到项目 → 完成任务 → 形成项目回顾。任一步刷新或换设备后仍能恢复状态。
2. 邮件行动项：用户授权并选择邮件 → 提取任务与截止时间 → 显示来源链接及拟议变更 → 确认创建任务 → 安排时间 → 回复邮件先生成草稿，再单独确认发送。邮件导入与 MCP 临时读取的语义保持可区分。
3. 编码修复：项目绑定仓库 → 建立隔离工作目录 → Agent 提议并执行受限命令 → 展示 diff 与测试证据 → 用户确认提交或保留工作区修改 → 关联任务。退出客户端不能丢掉运行记录，不能把任意远程终端当成沙箱。
4. 健康与计划：手机授权睡眠摘要 → 用户明确允许本次计划使用 → Hermes 提出时间安排建议 → 用户确认 → 改动允许修改的时间块。缺失数据标记未知，不推断疾病，不擅自移动会议。

每条流程还必须覆盖：重复点击、双窗口冲突、断网、令牌过期、撤权、服务重启、部分完成和跨用户访问拒绝。先通过流程验收，再扩大功能数量。

### 0.7 信息架构与交互

主导航收敛为今天、对话、项目/工作空间、计划、资料；连接、能力、执行记录和设置仍可直接进入，但作为基础设施入口而非抢占工作主线。导航迁移需保留旧入口跳转，避免突然打乱现有用户习惯。

- 对话：独立完整页面为主，侧栏为可选伴随模式；两者访问同一会话。会话草稿、附件、模型选择和滚动位置独立保存。
- 工作空间：项目概览与对话/任务/日历/文件分视图，同一资源通过深链接互相定位，不复制出第二份数据。
- 计划：任务收件箱、项目任务列表、日/周/月历；拖动安排生成 TimeBlock。自然语言输入先预览解析结果。
- 今天：下一步行动、时间冲突、待审批、正在执行和继续上次工作；完成感来自真实进度，不使用假统计。
- 全局搜索：按对象类型、项目和来源检索，结果遵循同一权限；先 SQL 搜索，规模和需求明确后再引入专用索引。
- 通知与操作中心：集中呈现同步失败、待确认、连接过期和运行结果；消息有明确目标对象和下一步动作。

跨端统一数据与操作语义，不强求完全相同布局。Windows 优先多任务、文件和开发；手机优先快速记录、语音、提醒、审批及原生健康。动画遵循统一时长和减少动态设置，不影响可操作性；覆盖键盘、焦点、屏幕阅读器、长文本和小屏布局。

### 0.8 连接器与扩展系统

将“已安装、已配置、已授权、验证通过、已启用、当前会话已加载”分开保存，UI 只显示对应事实。能力描述包含唯一标识、来源、版本、运行环境、依赖、输入输出 schema、权限、许可和健康检查。先做简单清晰的注册表，不先建设庞大插件市场。

官方连接器负责账户授权与业务同步；MCP 负责工具调用；Skill 负责流程指导。三者可打包为一个用户可添加的能力，但底层生命周期与安全边界独立。

安装采用持久化任务：校验来源/兼容性 → 权限预览 → 用户确认 → 安装/配置 → 授权 → 测试 → 启用。长任务返回任务 ID，支持断线后查询；不靠一个等待几十秒的 HTTP 请求承载整个安装。回滚只覆盖可回滚步骤，第三方授权等副作用需独立清理。

外部 Skill/代码包先校验文件路径、大小、版本、依赖和扫描结果；任何扫描都不能保证完全安全，执行仍需要权限与隔离。禁止客户端把任意安装命令交给工作台服务进程执行。

Google 服务需开发者应用注册、最小 scope、必要审核、令牌刷新及撤销；用户不应日常填写客户端密钥。HealthKit/Health Connect 必须由相应手机原生能力获取授权。Windows 网页不能通过一个插件自动绕过手机系统权限。

### 0.9 技术与迁移策略

当前继续使用 Web + Electron + Node 模块化单体，不立即迁移框架或拆微服务。先把领域逻辑从大路由/适配文件中按职责渐进抽离，保留 API 兼容和测试。Hermes 适配器负责协议与能力协商，不拥有任务、日历等产品数据。

单用户、自托管验证阶段可继续 SQLite；托管多实例前再明确 PostgreSQL、共享队列、对象存储及多租户运行时方案。不能把单进程锁当成分布式并发保护。手机客户端先验证 HealthKit/Health Connect、后台任务和通知，再通过独立架构决策确定跨端框架，不沿用旧文未经验证的 Flutter 结论。

迁移先扩展、后回填、再切换，保留旧字段读取兼容。旧任务 scheduledAt 回填为关联 TimeBlock，保留任务 ID、时区和完成状态；不能推断出不存在的独立会议。原文档归入默认工作空间，保留 ID 和版本；旧会话保留连接归属。迁移需有备份、恢复演练、数据数量/关系校验和明确的回退条件。

部署兼容性探针检查网关版本、端点、MCP HTTP 支持、OAuth、模型和语音能力；不支持的能力给出修复项。不得通过伪造已安装状态掩盖运行环境问题。

### 0.10 实施顺序与退出条件

| 阶段 | 交付范围 | 退出条件 |
| --- | --- | --- |
| A：领域基线 | 工作空间/项目、任务/事件/时间块、资源关联与兼容迁移 | 旧数据不丢；手动创建项目、安排任务、编辑文档形成可恢复闭环 |
| B：对话与受控操作 | 完整对话视图、独立草稿、上下文资源、提案/审批/执行记录 | Hermes 通过正式工具创建并安排任务；重复调用不重复写；撤权后拒绝访问 |
| C：可靠同步 | 持久化 outbox、冲突处理、Google 首个日历连接器、离线队列 | 双设备改期、删除、过期凭证、断网恢复全部通过；真实 Google 同步验收 |
| D：扩展生命周期 | 安装任务、Skills 导入、MCP 密钥/运行环境、版本升级与卸载 | 中断安装可恢复、来源与权限可审查、凭证不泄露、运行时兼容可诊断 |
| E：完整工作场景 | 邮件行动项、编码沙箱、连续语音、手机健康 | 各场景都有真实服务成功与失败验收，不能只靠 mock |
| F：发行 | 账户生命周期、签名更新、隐私合规、监控备份、计费如需要 | 发布审计、长稳/负载、恢复演练、客户端平台要求与商店审核 |

可提前并行做接口与原生能力验证，但不能跳过前置数据和权限模型，用 UI 占位冒充完成。每一阶段以验收结果而非预设开发天数决定是否进入下一阶段。

### 0.11 商业化持续门槛

所有新入口必须经过账户隔离、最小权限、输入校验、失败恢复和审计测试。高风险动作和私密数据的日志应默认脱敏；用户能够管理连接、导出自己的数据、注销账户及理解备份保留期。第三方授权和远端副本不能被宣称已随本地删除自动清除。

单用户 NAS 管理账号可以用于个人自托管，但不得直接扩展为不同付费用户共享的管理凭据。托管版需要每用户/工作空间隔离的运行时、凭证和资源限制；未经验证的远端 profiles 不能被当成可靠安全边界。

监控关注真实业务结果：任务写入与外部同步成功率、未知执行数量、冲突与重复记录、恢复耗时、授权失败率、AI 成本及关键页面响应。具体 SLO 在真实测试后制定，不虚构已达到商业指标。

参考项目的优点落实到结构与验收标准。许可限制见 REFERENCE_REVIEW.md；不复制未获商业授权的实现。保持一种统一设计语言，不把不同厂商界面机械拼装。

---

以下为早期规划，遇到冲突以上述修订为准。

日期：2026-09-18。目标：先个人真实使用，再逐步成为可上架的 Windows、Android、iOS 产品。

## 1. 决策与假设

- 产品核心：结合时间、任务、个人状态和资料，帮助用户计划、执行与复盘。
- 默认技术路线：Flutter 客户端、SQLite 本地数据库、TypeScript 单体同步服务、PostgreSQL、独立 Hermes 适配进程。开发前用小型原型验证原生插件。
- 产品面向三端，首个可用闭环先覆盖 Windows 和用户常用的手机平台。
- Hermes 指 https://github.com/NousResearch/hermes-agent 。后续绕过失效代理已成功读取官方目录、根 LICENSE 和桌面 package.json：根许可为 MIT，已有 Electron 桌面工程、MCP 和网关相关源码。具体接口兼容性仍需固定版本并联调；本文中的工作台工具名称仍为建议契约，不表示官方已有同名工具。第三方桌面参考见第 15 节。
- 暂按单人开发、以个人效率为主、可使用自己的模型凭据考虑。具体周期取决于开发经验、每周投入和实际数据源。
- 开始实现前必须固定 Hermes 版本，确认许可证、启动方式、会话调用、流式输出、工具扩展、取消与超时机制。不要以未经验证的 SDK 或 HTTP API 为前提。

## 2. 核心体验

早上：展示今天的会议、临近截止的任务、近期睡眠与活动摘要。Hermes 结合用户选择的工作时间和优先级提出当天计划。

白天：文字或系统分享快速收集事项；会议结束后手动输入要点或导入笔记，提取可确认的行动项；临时出现任务时，预览哪些安排需要调整。

晚上：比较计划和完成情况，处理未完成任务，记录主观精力，生成带来源的日复盘。

每周：检查项目进度、长期目标和时间分配，建议下周重点。健康数据作为生活安排参考，不作诊断或确定性能力判断。

产品验收的第一标准：连续使用两周，能够在工作台完成每日计划与调整，并且不会产生重复日程、丢失任务或悄悄修改安排。

## 3. 功能地图

| 模块 | 具体能力 | 优先级 |
| --- | --- | --- |
| 今天 | 日程与任务时间线、今日重点、状态摘要、同步新鲜度 | P0 |
| 收件箱 | 快速输入、分享接收、待整理事项、转任务或笔记 | P0 |
| 待办 | 标题、备注、状态、优先级、截止时间、预估时长、子任务 | P0 |
| 日历 | 接一个真实服务、空闲时段、冲突提示、专注时间块 | P0 |
| Hermes | 上下文选择、计划建议、变更预览、执行结果与来源 | P0 |
| 同步与恢复 | 离线读写、重试、冲突处理、导出、备份恢复 | P0 |
| 通知 | 任务与事件提醒、勿扰时间、跳转到具体事项 | P0 |
| 健康 | 首个手机平台的睡眠、步数、活动摘要、主观精力 | P0 验证，P1 完善 |
| 项目与目标 | 项目任务列表、下一步行动、周目标、进展复盘 | P1 |
| 笔记与资料 | Markdown 笔记、链接、附件、关联任务与项目、全文检索 | P1 |
| 例行事项 | 重复任务、习惯、晨间规划、晚间与每周复盘 | P1 |
| 自动化 | 条件触发、预览、授权范围、执行记录、失败处理 | P1 |
| 个人偏好 | 工作时段、专注偏好、提醒规则、可编辑的 AI 记忆 | P1 |
| 扩展连接器 | 第二日历、外部待办、邮件等 | P2 |
| 高级分析 | 时间分配、目标趋势、跨资料语义搜索 | P2 |

P0 健康验证必须早做，以免到后期才发现实际手表数据不可读取；健康接口不会阻塞不依赖它的任务和日历功能。

暂不纳入初期：财务账户聚合、社交消息全量归档、团队协作、完整密码管理、自动读取所有屏幕、无限制电脑控制和自研模型。每个连接器都带来长期兼容与维护成本。

## 4. 信息架构与三端分工

主导航：今天、收件箱、计划、项目、资料。健康与复盘初期作为今天页面的子视图，设置中管理连接器、同步、隐私和 Hermes。

Windows：宽屏时间线与任务列表，侧边 Hermes 面板，快速输入入口，文件导入，项目规划。初期不依赖常驻全局键盘监听或桌面控制。

手机：今天摘要、快速记录、提醒处理、健康授权和同步。后续提供系统分享入口、小组件、语音输入；语音录制必须有明确操作入口。

共同要求：离线状态清晰；日期与时间本地化；大字体和屏幕阅读器可用；展示数据来源、最后更新时间及 AI 操作状态；通知中的健康细节默认隐藏。

## 5. 技术架构

```text
Windows / Android / iOS Flutter 客户端
  本地 SQLite + 待同步操作队列 + 系统安全凭据存储
                    |
             TLS 认证与增量同步
                    |
单体应用服务
  身份与设备 / 同步 / 连接器 / 任务与日历 / 工作流
       |                |                  |
  PostgreSQL       外部日历服务       Hermes 适配进程
  附件存储                           受控的工作台工具

手机原生健康桥接 -> 本机数据聚合 -> 经授权的摘要同步
```

采用单体服务和数据库任务表起步。暂不引入微服务、Kafka 或独立向量数据库。服务端定时工作由持久化任务驱动，不依赖某个手机进程持续运行。

Flutter 能共享业务与大部分 UI，但 HealthKit、Health Connect、通知、后台任务、密钥存储和桌面能力必须分别验证。iOS 开发、构建和签名需要 macOS/Xcode，以及真机测试。

个人版可将服务和 Hermes 部署在自用电脑、家庭服务器或 VPS。部署在休眠的 Windows 电脑上意味着远程 AI 和定时工作可能不可用；客户端应仍能离线查看、记录和本地提醒。需要稳定后台执行时，再迁移常开主机。

后续提供自托管与托管两种部署。个人凭据放在系统凭据库或服务端密钥存储中，不打包进客户端、不写入 Git 或日志。远程访问通过经认证的加密连接，不公开暴露 Hermes 管理端口。

## 6. 数据与同步设计

基础实体：Task、Project、Goal、CalendarEvent、TimeBlock、Note、Attachment、HealthDailySummary、CheckIn、Routine、Review、Preference、Connection、AgentRun、ActionProposal、AuditEvent。

任务、截止时间和日历时间块分开建模：任务可以没有安排；一个任务可以有多个工作时间块；移动时间块不改变任务截止时间。

所有业务实体保留稳定 ID、所属用户、版本、创建及修改时间、删除标记。外部实体额外记录 provider、account、external_id、远端版本和同步游标。数据库唯一约束与幂等请求共同防止重复导入和重复创建。

客户端先写入本地事务和 outbox，后台上传；服务端返回变化游标，客户端增量拉取。删除传播使用 tombstone，明确保留期以及长期离线设备重新同步策略。服务器给出规范版本，不以设备时钟作为唯一冲突依据。

冲突规则：不冲突字段可合并；同时编辑同一任务的重要字段时保留双方版本并提示选择；外部日历先校验远端版本，不静默覆盖别人或其他客户端刚改过的内容。

时间设计：保存时间点和事件时区；全天事件保存日期语义；重复事件保留规则及单次例外；测试跨时区、夏令时、改期、取消和重复实例。初期写入只支持本产品创建的普通时间块，复杂重复事件先只读。

健康数据保留来源设备、样本区间和新鲜度。优先使用系统提供的聚合与来源处理能力；无法确认来源优先级时让用户选择，禁止把多设备步数简单相加。缺失或未授权数据表示未知，不能表示为零。

备份包含结构化数据、附件索引和版本信息；支持 JSON/Markdown/ICS 等适用格式导出。备份应验证恢复过程，不能只有“导出成功”提示。

## 7. Hermes 集成契约

先做一个最小探针：让固定版本 Hermes 接收请求，读取一个测试日程，返回结构化计划，再调用受限测试工具创建任务。验证流式状态、并发会话、进程退出、取消和断连。

根据官方实际能力选择稳定的 API、工具扩展或 MCP 适配。如果只有 CLI，先评估结构化输入输出和进程生命周期管理；解析终端自然语言输出只能作为实验，不能成为生产协议。MCP 若可用，也只是工具通信层，不代替鉴权和会话管理。

建议由工作台提供的工具契约：

| 工具 | 行为 |
| --- | --- |
| get_today_context | 按授权返回今日任务、事件、偏好和可选健康摘要 |
| search_personal_data | 在指定数据范围检索，返回来源和更新时间 |
| find_free_slots | 按工作时间与日历确定性计算可用时间 |
| propose_plan | 保存结构化计划，尚不修改外部服务 |
| create_task | 在授权范围内创建任务，要求幂等键 |
| apply_approved_actions | 校验有效授权、数据版本与动作范围后执行 |
| get_action_status | 查询部分成功、失败和重试状态 |

这些是产品需要实现的接口名称，不是声称 Hermes 已有同名工具。

AI 提案应包含：拟修改对象、前后值、原因、引用来源、依赖的数据版本和有效期。执行前重新检测冲突；数据变化时重新预览。接受计划和写入成功是两个不同状态。

工作流由应用保存状态：提案、已授权、执行中、成功、部分成功、失败、取消。外部 API 无法保证多操作原子事务，必须记录逐项结果。撤销属于补偿操作，只有远端状态仍满足条件时才执行；邀请等已经产生的外部影响未必可撤销。

默认只暴露工作台需要的工具。如果 Hermes 提供 shell、浏览器或文件工具，应使用隔离运行环境和有限目录，不让普通日程安排自动获得整台电脑权限。

个人记忆分成事实、偏好和推断。事实附来源；偏好可编辑；推断有时间范围和确认状态。不要把 AI 对话记忆作为唯一任务数据库。任务和日程的事实以工作台与其原始服务为准。

## 8. 健康、日历和其他接入

iOS：使用 HealthKit，按需请求读取权限。睡眠等数据是否可获得取决于用户实际设备与数据源。后台交付受系统策略影响，不能保证实时上报。

Android：使用 Health Connect，验证设备、系统版本、权限和实际健康应用的数据写入情况。对不提供所需数据的设备给出清晰状态，必要时后续研究厂商接口。

日历：先按个人账户选择 Google Calendar 或 Microsoft Graph；Apple 本地日历可评估 EventKit，但它不是一个可直接替代跨端服务器日历接口的云 API。首期以一个日历来源为主。

待办：内置待办是主数据源。接入 Todoist 或 Microsoft To Do 时，先定义来源归属、完成状态映射和冲突处理，再开放双向同步。

资料：优先手动输入和用户选择的导入。初期使用 SQLite/PostgreSQL 全文检索，检索质量不足再加向量索引；删除资料时同步清理摘要与索引。

邮件：后期先做用户选择邮件转任务，避免初期全量读取邮箱。消息、网页和笔记属于不可信外部内容，不能通过其中的指令提高工具权限或绕过授权。

## 9. 自动化与提醒

第一批自动化：晨间规划草案、晚间复盘、周回顾、临期任务提示。新增会议影响计划时生成调整建议，不默认移动他人邀请的会议。

每条规则定义触发时机、读取范围、可执行动作、安静时段、执行次数限制和通知条件。相同事件去重；失败重试有上限；没有重要变化时不反复提醒。

当地时间的周期规则应保留时区，并在出行或夏令时变化时按规则重新计算。通知是尽力送达的通道，应用内仍应保留待处理事项。

个人版可先使用本地提醒；托管版再增加 APNs/FCM 等平台推送，并评估目标地区 Android 推送可达性。AI 无法响应时，已有任务、日历、提醒仍正常工作。

## 10. 隐私与产品化边界

健康原始明细默认留在手机。同步摘要与发送给模型分别授权；说明数据经过自托管主机还是云服务、模型提供商能接收哪些内容。用户可禁用健康上下文而继续使用其他模块。

本地敏感存储采用受支持的加密数据库或平台文件保护，密钥放系统安全存储。服务端传输和静态加密作为基础能力；不能因此宣称端到端加密。若未来要支持真正的端到端加密，需要明确服务端 Hermes 如何取得用户授权的解密上下文。

提供连接器断开、设备撤销、权限查看、数据导出和删除。产品化删除范围包括业务数据、附件、索引、AI 记忆和备份的到期清理策略。日志默认不记录健康明细、OAuth token 或完整提示词。

上架前核对当期平台规则：健康数据使用与披露、后台权限、账户删除、隐私标签、模型数据处理披露、代码签名、软件依赖与 Hermes 许可证、销售地区规则。应用内数字服务收费路径根据各商店及地区规则评估，不预先假定外部付款可通用。

托管版每个用户的数据、凭据、检索和 Agent 会话必须隔离。不要把不同用户放进共享可写的 Hermes 工作目录。数据库必须执行用户范围检查，Agent 进程也需隔离与资源限制。

## 11. 开发阶段与验收

| 阶段 | 产物 | 验收门槛 |
| --- | --- | --- |
| A：可行性探针 | Hermes 调用、一个日历、实际手机健康读取、三端空壳构建 | 明确每个接口可做什么，跑通真机和固定版本 |
| B：每日工作台 | Windows 与主手机端的今日页、待办、日历、离线与同步 | 跨端新建完成任务不丢失，断网恢复不重复 |
| C：AI 闭环 | 带来源的计划、修改预览、受控写入、执行记录 | 过期计划拒绝执行，重试不重复创建，部分失败可处理 |
| D：个人长期使用 | 健康摘要、项目、笔记、复盘、提醒、备份恢复 | 连续两周日用；恢复到另一台设备；模型断线仍能办公 |
| E：第三端和内测 | 另一手机平台、安装与更新、迁移、可访问性 | 真实设备测试，升级保留数据，权限拒绝与撤销正常 |
| F：上架准备 | 账户生命周期、隔离、隐私说明、支持渠道、商店素材 | 真实试用反馈收敛，依赖许可与当期商店要求完成核对 |

估算前提：熟悉相关技术的单人全职开发、每类先接一个来源、UI 范围受控。A 约 1–2 周，B 约 3–5 周，C 约 2–3 周，D 约 3–5 周；E 和 F 合计再预留 4–8 周，商店审核时间另算。这是工作量区间，不是交付承诺，插件或厂商权限问题会改变排期。

个人可用版本与商业发布版本分开验收。不要等所有 P1/P2 都完成才开始使用。

## 12. 测试与运行质量

- 同步：双端同时编辑、离线新增和删除、重复上传、长时间离线、服务端恢复。
- 日历：跨时区、全天、夏令时、重复例外、凭据失效、连接器限流。
- AI：虚构来源、无权限读取、恶意笔记指令、重复工具调用、计划过期、取消和部分执行。
- 健康：拒绝授权、撤销、无数据、多设备来源、跨日睡眠、旧摘要。
- 移动端：后台被终止、低电量、无网络、通知禁用、分享入口、真实设备布局。
- 发布：数据库迁移失败保护、备份恢复、用户删除、跨用户访问阻断。

运行时记录去敏后的同步错误、连接器状态、动作成功率、模型延迟和费用。先在本地记录必要诊断，后续遥测明确告知用户。

## 13. 成本与后续商业模式

初期主要成本：开发时间、测试设备或 Mac、模型调用、可选常开主机及开发者账户。模型预算按每次调用的上下文规模和每天规划次数实测，设置单日上限；简单统计、提醒与冲突检测使用代码完成。

先用个人使用验证留存价值。后续可探索基础本地功能、自托管或自带模型凭据，以及包含托管同步、备份和 AI 配额的订阅。避免无限额度承诺。上架版必须有完整的配置、错误提示和普通用户能理解的部署路径。

产品指标优先看：实际每周使用天数、被采纳的计划比例、人工修正次数、同步错误、重复写入和恢复能力。功能数量不作为成功指标。

## 14. 开始实施前的最少输入

1. 主力手机型号、系统、手表或健康数据应用。
2. 现有日历和待办服务，以及是否需要保留原服务作为主来源。
3. Hermes 是否已安装、运行主机和模型配置；不需要提供密钥。
4. 开发经验、每周可投入时间、是否具备 Mac/iPhone 测试条件。
5. 是否愿意使用常开服务器，以及健康摘要能否离开手机。

这些信息决定首个连接器和部署方式，不改变整体产品方向。下一步应先完成阶段 A，并据结果细化数据协议和第一版界面。

## 15. Hermes Desktop 源码参考评估

核查日期：2026-09-18。第三方仓库：https://github.com/jeromeleeqy-hash/Hermes-Desktop 。核查提交：013420f420f14a589caa359e0346c73bb5bfb49e。本节基于 README、文件目录和关键源码阅读，未安装应用、运行测试或连接真实 Hermes 服务。

### 已确认事实

- build.gradle.kts 使用 Kotlin JVM 2.3.20、Compose 1.10.1、JDK 21、OkHttp，以及 JavaFX/JNA 等桌面依赖；打包目标为 Windows 和 macOS。当前构建不是已经配置 Android/iOS 的 Kotlin Multiplatform 工程。
- README 标记 1.8.4，而所读提交的构建配置为 1.8.8；评估与复用必须固定提交，不能混用 README 与发布包版本信息。
- data/HermesApiClient.kt 通过 HTTP 管理会话、配置、文件和定时任务，通过带票据的 /api/ws 使用 JSON-RPC 2.0。可见 /api/status、/api/auth/ws-ticket、session.resume、session.status、approval 和 clarify 等调用及处理逻辑。
- 客户端读取网关版本与 capabilities，并针对部分缺失端点降级。这种能力协商比根据版本号猜功能更值得借鉴。
- desktop/DesktopTaskBoard.kt 将待处理审批或澄清、正在运行的 Agent 会话和最近完成记录展示为三列。它不是带截止时间、重复规则和日历安排的个人待办系统。
- 会话恢复代码和历史功能文档重视草稿、附件及运行状态保留；读取连接状态与重发不确定任务区别处理。仍需真实网关联调验证，不应仅凭客户端代码认定已经实现端到端恰好一次执行。
- THIRD_PARTY_NOTICES.md 说明协议、模型、部分解析逻辑和素材来自 hermes-assistant-2.zip（标记 Android 3.6.5），未为其重新指定许可。在所查目录未发现项目自身的明确许可，GitHub license 字段也为 null；第三方依赖的许可不能替代本项目的代码授权。

### 对个人工作台的取舍

直接借鉴设计：统一 AI 执行中心、审批与澄清状态、来源跳转、草稿恢复、能力协商、文件成果关联、桌面快速入口。代码移植需先确认授权与技术兼容。

独立建设：个人待办、日历时间块、健康摘要、跨设备离线同步、项目目标、健康数据授权和备份恢复。用户任务与 AgentRun 分开建模，并用关联 ID 连接。

网关协议可作为接口验证清单，但必须与选定官方 Hermes 版本实际对照；本次未验证这个客户端的全部端点是否与官方部署一致。

### 官方工程带来的方案调整

官方仓库 apps/desktop/package.json 已有 Electron 桌面工程和 React 相关依赖，根 LICENSE 为 MIT。它应成为客户端协议、会话生命周期与桌面运行管理的优先对照来源；具体子目录、依赖和品牌素材仍需检查是否有额外条款。

保留 Flutter 作为三端共享体验的候选；同时新增“复用官方 Electron 桌面、移动端单独实现”的候选。前者适合以健康和日程为中心统一三端，后者适合优先扩展已有桌面 AI 能力。先比较接入原型和维护成本，再定栈，不因第三方项目的框架单独改变技术路线。

### 核查来源

- 第三方构建：https://github.com/jeromeleeqy-hash/Hermes-Desktop/blob/013420f420f14a589caa359e0346c73bb5bfb49e/build.gradle.kts
- 第三方网关客户端：https://github.com/jeromeleeqy-hash/Hermes-Desktop/blob/013420f420f14a589caa359e0346c73bb5bfb49e/src/main/kotlin/com/qingyu/hermescompanion/data/HermesApiClient.kt
- 第三方任务中心：https://github.com/jeromeleeqy-hash/Hermes-Desktop/blob/013420f420f14a589caa359e0346c73bb5bfb49e/src/main/kotlin/com/qingyu/hermescompanion/desktop/DesktopTaskBoard.kt
- 第三方来源声明：https://github.com/jeromeleeqy-hash/Hermes-Desktop/blob/013420f420f14a589caa359e0346c73bb5bfb49e/THIRD_PARTY_NOTICES.md
- 官方桌面工程：https://github.com/NousResearch/hermes-agent/tree/main/apps/desktop
- 官方根许可：https://github.com/NousResearch/hermes-agent/blob/main/LICENSE
# Implementation Checkpoint: Project Workspace

2026-09-18 follow-up: schema 6 adds reviewed reply-to-task imports with atomic project links, persistent provenance and idempotent receipts. Users review explicit unchecked Markdown tasks before creating them; dates are explicitly entered, not inferred. This is a human-approved result capture flow, not an autonomous Hermes tool bridge. Unit/API tests and isolated browser flows pass; live model generation remains an acceptance step.

2026-09-18: Phase A has its first working slice: schema 5 projects and shared resource links, atomic task creation/linking, project archive/restore, document navigation, explicit read-only context attachment, and recoverable metadata conflicts. Existing task/calendar records remain authoritative. This does not complete Phase A: standalone events/time blocks, persistent conversation ownership and the unified AI command layer remain pending. Third-party sync and native health integrations are not implemented by this slice.
