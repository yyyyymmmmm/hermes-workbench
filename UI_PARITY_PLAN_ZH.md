# 工作台与 UI 原型完整对照方案

## 2026-09-18 实施进度

- 已完成：同账号、同连接、default 档案的共享内存缓存；首次自动加载、请求合并、串行读取、TTL、失败保留旧数据、切换账号清空。目录 10 分钟，MCP 30 秒，其他资源 60 秒。
- 已完成：能力和连接双列卡片、手机单列、彩色图标、收起的管理区、统一外部 MCP 新增入口；搜索、分页、OAuth、安装、验证、移除保留。
- 已完成：Hermes 各设置分类共用状态头；可写工具集复选框及回读验证；SOUL 正文编辑与草稿保留。
- 已完成：原生定时任务新建、编辑、删除、暂停/恢复，详情显示下次执行、上次状态及运行记录。新建需明确确认，按服务器时区和默认启用策略运行，默认结果投递至 local。
- 安全边界：SOUL/任务更新使用写前版本比对和写后回读；原生接口不提供原子 CAS，不能保证跨客户端同时写入完全无竞争。新建任务使用持久化请求凭据，结果不明时不自动重试。
- 尚未完成：MEMORY.md、USER.md 正文管理。已对 NAS 进行只读探测：原生 /api/memory 提供服务状态与文件计数，Studio 的 /api/hermes/memory 在原生返回 404；不能把 SOUL 编辑或记忆开关当成完整记忆正文管理。
- 尚未覆盖：原生手机真机弱网/后台续流测试；本次没有修改 NAS 上的真实记忆、工具开关或定时任务来做测试。远端写入通过模拟接口的单元和浏览器测试验证。

以下内容仍是完整目标；以上进度不代表所有目标已经实现。

## 原则与事实

以 ui/index.html 引用的 ui/terminal/views.js、features.js、styles.css 为视觉基准。
保留所有已接通功能，包括模型、档案、外部 Agent、语音、权限、安装、附件、健康、日历和待办。
本文件是待实施方案，不代表这些管理功能已经完成。

原型的能力中心是卡片网格；连接页原本是设置行。本次按用户要求，把连接页也升级为桌面每行两张卡片。
原型的 SOUL/MEMORY 编辑器和定时规则只有本地保存，并不是真实远端记忆或后台调度。
需要保留这些成熟的操作入口，再补完整的远端接口，而不是退化成只读状态。

## 逐项对照

| 区域 | 当前正式版问题 | 按原型恢复并完善 |
| --- | --- | --- |
| Hermes 设置 | 多处分散的“读取服务器”、内容宽度和操作不统一 | 状态头部、固定分类标签、统一设置宽度、页面级刷新 |
| 工具权限 | 部分工具组只有状态，没有开关 | 图标、说明、复选框；只对远端确实支持写入的范围开放操作 |
| Skills/MCP | 授权、验证、启用、删除挤在列表里 | 卡片显示身份与状态，一个管理按钮，详情面板承载完整流程 |
| 记忆与设定 | 只有启用开关、字符上限等参数 | 分为 SOUL、MEMORY、用户画像和运行参数；真实读取和修改内容 |
| 定时任务 | 只能列出、暂停、恢复 | 新建、编辑、删除、下一次执行、运行历史、错误详情 |
| 能力中心 | 复用了设置页长列表 | 彩色图标、两列卡片、共享搜索筛选、顶部统一新增 |
| 连接与数据 | 混合状态行、未接入占位、内嵌配置表单 | 两列账号/数据源卡片；授权、同步、断开等操作进入管理详情 |

## 读取和缓存

当前 agent.js 已有会话内缓存，但没有统一的新鲜度策略和跨页面资源仓库。
app.js 每 15 秒请求工作台接口；其中 /hermes 摘要读取本地连接信息，不能把这些请求全部算作访问 NAS。

1. 三个页面共用资源仓库，缓存键包含账号、连接 ID、Agent 档案和资源类型。
2. 有缓存先显示，不白屏；第一次进入自动加载骨架，不要求用户先按“读取”。
3. 同资源并发读取合并为一次；有效期内切换标签不重复访问远端。
4. 初始建议：工具和已安装能力 60 秒，连接状态 30 秒，目录 10 分钟。后续按实测调整。
5. 过期后后台更新，失败保留旧内容并显示更新时间、错误和重试，不阻断操作页面。
6. 修改成功只刷新受影响资源，并读取远端结果核实；不得清空整个设置页。
7. 未保存表单与服务器快照分开保存。后台刷新不能覆盖用户正在编辑的内容。
8. 退出或换连接清除对应缓存。默认仅持久化非敏感目录元数据，不缓存密钥、健康数据或原始服务器配置。
9. 缓存不作为执行权限依据。每次写入仍由后端校验账号、项目、档案和远端版本。

## 记忆管理闭环

- 内容与参数分开：SOUL、MEMORY、用户画像编辑器，另设记忆引擎参数。
- 先探测 NAS 支持的接口；不能凭假定文件路径直接写入用户服务器。
- 读取内容和版本，保存前展示差异，按版本防止并发覆盖，保存后回读验证。
- 支持未保存状态、冲突处理，以及远端真正支持的历史恢复。
- 明确作用的档案和共享范围；保存不等于当前正在运行的会话立即重新载入。
- 不把本地草稿称为“已同步记忆”。缺少适配能力时说明依赖，不做假成功。

## 定时任务闭环

- 新建表单包含名称、任务指令、执行档案、时间规则、时区及远端支持的结果投递方式。
- 展示下次执行、上次执行、失败原因与实际执行记录。
- 明确夏令时、错过执行时间的处理方式，不让手机本地时间冒充服务器时区。
- 新建后默认停用或单独确认启用；可能调用工具、产生费用，保存规则不等于无限授权。
- 增删改、暂停和恢复都要回读确认，以远端任务 ID 防重复。
- 不另造一套同名本地定时器，与 Hermes 产生双重执行。

## 卡片和布局规范

- 桌面两列，手机一列。页头保留一个固定新增按钮，不在每一段重复放完整新增表单。
- 共享搜索、类型筛选和已安装筛选；长列表分页并保留滚动位置。
- 卡片只显示图标、名称、类型、来源、状态、简短说明和添加/管理主入口。
- 详情面板承载权限、OAuth、连接测试、配置和卸载；关闭后返回原卡片并恢复焦点。
- 使用原型的细边界、低饱和图标色和 7–8px 圆角，不套多层装饰卡片。
- 动画遵循减少动态效果设置，不用虚假进度掩盖网络等待。

## 实施顺序与验收

1. 统一缓存和请求去重；测量每次页面切换的实际远端请求数量。
2. 恢复 Hermes 设置框架和已安装能力管理入口。
3. 补齐记忆内容与定时任务远端读写，再接编辑器和任务详情。
4. 能力中心与连接页统一改为双列卡片与顶部新增。
5. 验证全部主题、中英文、桌面/手机、失败/拒绝授权、跨账号、并发编辑。

验收必须满足：新鲜缓存不重复读取；换账号不闪出前一账号数据；刷新失败不清空列表；
表单草稿不被覆盖；创建规则确实存在于 Hermes；记忆保存后远端内容一致；
授权完成回到原卡片；没有任何现有功能因改版消失。

## 本轮 NAS 性能样本

相同短提示、无工具、新建会话，测速前后配置均为 gpt-5.6-terra。
网关未返回实际会话模型，不能断言供应商没有内部路由变化。

| 方式 | 获取票据与连接 | 首段正文 | 总耗时 |
| --- | --- | --- | --- |
| 直连 NAS，新连接 | 20ms | 4171ms | 4570ms |
| 连接池，首次连接 | 18ms | 5329ms | 5373ms |
| 连接池，复用连接 | 0ms | 4727ms | 5673ms |

这是三次单样本，不是稳定的性能结论，也不是 Hermes Studio 页面端到端测速。
只证明本次握手开销被消除，不能证明总推理时间下降。
移动端此次改为原生 SSE 字节流；真机弱网、后台恢复和 85 秒原生连接超时后的续传仍需设备验证。
